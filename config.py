"""Централизованные настройки торгового движка.

Здесь собраны все «магические числа» стратегий: таймфреймы, глубина истории,
окна поиска уровней, пороги объёма и пробоя, интервалы планировщика. Раньше они
были разбросаны по коду — теперь в одном месте, чтобы легко калибровать.

Через /settings пользователь крутит ровно ДВА значения — фильтры строгости отбора
(MAX_ENTRY_DIST_ATR и MAX_RISK_ATR, см. _DEFAULTS внизу файла). Они хранятся в
settings.json рядом с кодом. Всё остальное — константы, меняются правкой этого файла.

Все пороги детектора заданы В ДОЛЯХ ATR (BREAK_ATR, STOP_ATR и оба фильтра строгости),
а не в процентах цены. Единая мерка на все инструменты — см. комментарий у BREAK_ATR.
"""

import json
from pathlib import Path

# ── Таймфреймы и глубина истории ────────────────────────────────────────────
D1_TIMEFRAME = "1d"
H1_TIMEFRAME = "1h"
M15_TIMEFRAME = "15m"
D1_LIMIT = 30          # свечей D1 для глобального контекста (стратегия №1)
H1_LIMIT = 120         # свечей H1 для локальной картины (~5 дней)
M15_LIMIT = H1_LIMIT * 4 + 8   # пятнадцатиминуток на H1_LIMIT скользящих часов

# ── СКОЛЬЗЯЩИЙ ЧАС (3 сентября 2026) ────────────────────────────────────────
# Движок ищет сигнал по свече ДЛИНОЙ ЧАС — но собирает её заново каждые 15 минут,
# а не ждёт круглого часа. Час кончается на последней закрытой пятнадцатиминутке.
#
# ЗАЧЕМ. Для движка час — это ДЛИНА свечи, а не положение стрелок: средний объём,
# ATR, размах и сила отбоя считаются по свече той же длины и от сдвига границы не
# меняются. А ждать конца календарного часа приходится до 60 минут, и это дорого.
# Замер 3 сентября 2026 (лаборатория, `m15_signal.py`, 14 инструментов, 84 дня):
# тот же сигнал, но вход в момент, когда сетап уже виден, лучше входа по закрытию
# часа на +0.36 R НА СДЕЛКУ при t 13.5 — самая крупная величина за всю историю
# замеров проекта (лучший рычаг отбора, «сила отбоя», дал +0.020).
#
# ЧЕГО ЭТО НЕ ДАЁТ. Те +0.36 R — верхняя граница с подглядыванием, и собрать её
# нечем: все честные ранние триггеры вышли ХУЖЕ якоря. Скользящий час — самый
# дешёвый из них:
#
#   вариант (нетто R на сделку)             сигн./мес   нетто
#   сейчас, по закрытию календарного часа        326    -0.067
#   СКОЛЬЗЯЩИЙ ЧАС  <- принято                   584    -0.090
#   детектор по M15, геометрия часа              755    -0.113
#   детектор по M15 целиком                      785    -0.160
#
# Разница со старым поведением (0.023 R) ЛЕЖИТ ВНУТРИ ШУМА этих данных: четыре
# фазы скользящего часа (:00/:15/:30/:45) — это одна и та же стратегия, снятая
# четырьмя способами на одном периоде, и их брутто разошлось на ±0.036. То есть
# «хуже» на таких данных не отличить от «не хуже», и решение вшить было принято
# ВЛАДЕЛЬЦЕМ как продуктовое: задержка сигнала падает вдвое (медиана 15 минут
# вместо 30), сигналов становится в 1.8 раза больше.
#
# ═══ ВЫКЛЮЧЕНО 7 сентября 2026 — решение владельца. Движок снова смотрит
# КАЛЕНДАРНУЮ часовую свечу биржи и даёт сигнал по её закрытию. ═══
#
# Мотив тот же, что у возврата объёма на свечу пробоя (см. SWEEP_WINDOW), и он
# последователен: скользящий час — это ПОБЛАЖКА ОТБОРУ. Движок проверяет сетап
# четыре раза за час вместо одного, и у сетапа четыре попытки пройти пороги
# вместо одной. Отсюда и «в 1.8 раза больше сигналов»: правила те же, а сито
# просеивают чаще. Владелец убирает поблажки, возвращая июньскую строгость.
#
# ЦЕНА, названная до правки: задержка сигнала растёт с 15 минут обратно до часа
# (медиана 30 минут), сигналов становится примерно вдвое меньше. Это ровно та
# задержка, из-за которой скользящий час и вводили 3 сентября.
#
# ЧТО ГОВОРЯТ ЗАМЕРЫ: ничего определённого, и оба — внутри собственного шума.
#   • m15_signal.py (3 сентября, 14 инструментов, 84 дня): календарный час
#     −0.067 против −0.090 у скользящего, то есть календарный ЛУЧШЕ;
#   • step5.py (7 сентября, GOLD+BRENT, 29 дней): календарный −0.303 против
#     −0.197 у шага 15 минут, то есть календарный ХУЖЕ.
# Направления противоположные, и оба меньше разброса фаз (±0.036 и ±0.185
# соответственно). Выбрать между «лучше» и «хуже» эти данные не позволяют —
# решение продуктовое, а не статистическое.
#
# True — вернуть скользящий час. Ничего, кроме источника свечей движка, при этом
# не меняется: уровни и тренд считаются по обычным часовым свечам в обоих
# режимах (в замере это отдельный арм F2, он совпал с основным). Сборка
# analyzer.rolling_hours и пять её тестов остаются в коде нетронутыми.
ROLLING_HOUR = False

# ── ЛОЖНЫЙ ПРОБОЙ (Spring/Upthrust): В БОЮ ПО ПРАВИЛАМ 23 ИЮНЯ — 15 сентября 2026 ──
# Утром 15 сентября владелец снял ложный пробой с боя (SPRING_SIGNALS = False), в тот же
# день вернул — но в редакции 23 июня 2026, то есть правилами первого движка:
# SPRING_ENGINE = "june23" (модуль spring_june). Сентябрьский движок (pattern_detector:
# сила отбоя, свежее пересечение, пулы, пороги в ATR, минимальная цель) из кода НЕ
# удалён — любое другое значение SPRING_ENGINE возвращает его без правок.
#
# Какой модуль шлёт сигналы, тот же разбирает расклад в /analyze (scheduler.spring_rules):
# иначе отчёт обещал бы сигнал по одним правилам, а бот слал бы по другим.
#
# ЦЕНА, названная до включения: на истории BingX эти правила дают около 500 сигналов в
# месяц, винрейт 57%, брутто −0.057 R и нетто −0.254 R на сделку; счёт 1000$ при риске 1%
# уходит в ноль (CLAUDE.md, «Замер 21 августа 2026 — ПЕРВЫЙ ДВИЖОК»).
#
# SPRING_SIGNALS = False снимает задачи run_analysis и monitor_signals (scheduler.jobs);
# уже открытые сигналы track_signals доводит до исхода при любом значении.
#
# ВАРИАНТ НА ЗАМЕНУ ЛЕЖИТ ОТДЕЛЬНО и в бой не выбран: ветка rr-filter-and-m15-early
# (коммит 817e3ef) — подпись запасной цели, фильтр R:R 1:2, ранние ловушки M15.
SPRING_SIGNALS = True
SPRING_ENGINE = "june23"
BREAK_PCT = 0.0005      # правила 23 июня: глубина прокола — доля ЦЕНЫ уровня (0.05%)
STOP_SPREAD = 0.001     # правила 23 июня: запас стопа за фитилём свечи — доля цены (0.1%)

# НАПРАВЛЕНИЕ ДВИЖОК СМОТРИТ ПО 6-ЧАСОВЫМ СВЕЧАМ (16 сентября 2026, решение владельца).
# Правило прежнее — EMA20, цена против средней плюс её наклон (analyzer.engine_trend);
# сменился только таймфрейм, на котором оно считается. До этого дня направление бралось
# с ДНЕВНЫХ свечей.
#
# ЗАМЕР (june_dir2.py, 13 монет + GOLD, 27 мес., якорь воспроизвёл 13644 сделки и
# брутто -0.057R; самопроверка: EMA20 на 24-часовых свечах повторила боевой дневной
# фильтр — -0.058 против -0.059). Перебраны три семейства мерок направления и около
# тридцати окон:
#   дневной фильтр (было)        462 сигн./мес   брутто -0.059   экзамен -0.034
#   EMA20 на 6-часовых (стало)   469 сигн./мес   брутто -0.037   экзамен -0.001
#   канал 6-8 часов              419-434/мес     брутто -0.031   экзамен -0.013
# Шестичасовое окно выбрано не как лучшая строка по брутто (канал чуть лучше), а как
# лучшая ПО СОВОКУПНОСТИ: оно единственное не режет частоту сигналов и лучше базы на
# ОБЕИХ выборках. Дневное окно оказалось худшим из проверенных: оно выбрасывало сделки
# ЛУЧШЕ тех, что оставляло (-1.7 своих ошибки).
#
# ЦЕНА РЕШЕНИЯ НАЗВАНА: преимущество НЕ ДОКАЗАНО. На полной истории оно 1.6 своих
# ошибки, на экзамене после 01.02.2026 — 1.3 при пороге 2. Из пяти пунктов критерия
# пройдено четыре: дуга по соседним окнам связная, оптимум внутренний, обе половины
# и экзамен в одну сторону, но порог значимости не взят. Брутто остаётся отрицательным.
# СТОП ЗА ФИТИЛЁМ + 1 ATR и МИНИМАЛЬНАЯ ЦЕЛЬ ПО КЛАССАМ (17 сентября 2026, решение
# владельца). Две правки в июньских правилах, обе в спросе «меньше вылетать по стопу».
#
# 1. СТОП. Был «за фитилём + 0.1% цены», стал «за фитилём + JUNE_STOP_ATR × ATR».
#    Замер june_stop.py (13 монет + GOLD, 27 мес., якорь 12778 сигналов / брутто
#    -0.037): стопов 40% -> 26%, напрасных (выбило, а цель потом дошла) 25% -> 12%,
#    брутто -0.037 -> -0.008 (t +2.7 — единственная строка сетки, взявшая порог),
#    нетто -0.213 -> -0.087. Обе половины истории и экзамен в ту же сторону.
#    Перевод порогов в ATR сам по себе не помогает: эквивалент 0.10 ATR дал -0.041,
#    чуть ХУЖЕ процентов (june_atr.py). Работает именно ШИРИНА: риск растёт с 1.41%
#    до 2.37% цены, комиссия делится на него и сделка дешевеет. Это восьмое подряд
#    воспроизведение «издержек в R», а не найденное преимущество.
#
# 2. МИНИМАЛЬНАЯ ЦЕЛЬ. Уровни ближе JUNE_MIN_TP_R × риск при выборе цели пропускаются,
#    цель встаёт на следующий за ними. Число сигналов при этом НЕ меняется — двигается
#    только цель (как MIN_TARGET_ATR у сентябрьского движка).
#    РИСК ДЛЯ ЭТОЙ МЕРКИ СЧИТАЕТСЯ ПО СТАРОМУ СТОПУ (фитиль + STOP_SPREAD), а не по
#    новому широкому — прямое указание владельца. Иначе рычагов стало бы два: цель
#    поехала бы вслед за стопом и отодвинулась вдвое.
#
# Замер june_tp.py по классам (стоп 1 ATR везде):
#   крипта, цель ≥ 1.0 риска    454 сигн./мес  брутто +0.005  нетто -0.084 (было -0.211)
#   золото и нефть, цель ≥ 0.5  62 сигн./мес   брутто -0.001  нетто -0.132 (было -0.267)
#   валюта, цель ≥ 0.5 риска     83 сигн./мес  брутто +0.008  нетто -0.407 (было -0.394)
# Итог -1477 R против -3121 R у прежней конфигурации.
#
# ЗОЛОТО И НЕФТЬ ПОЛУЧИЛИ МИНИМУМ ТОЖЕ (вечер 17 сентября, решение владельца; днём он
# просил их не трогать). Величину он не назвал, взята 0.5 — лучшая из трёх строк замера
# для товаров: без правила нетто -0.136 и брутто -0.017, при 0.5 нетто -0.132 и брутто
# -0.001, при 1.0 нетто -0.150 и брутто -0.010. Заодно это убирает сигналы вроде
# «риск 93 доллара ради 2» (R:R 0.02), которые у золота вылезали из-за широкого стопа
# при отсутствии минимума.
#
# ЦЕНА РЕШЕНИЯ НАЗВАНА. У ВАЛЮТЫ минимальная цель нетто УХУДШАЕТ: один стоп 1 ATR без
# неё даёт -0.374, с целью ≥ 0.5 риска -0.407 (нынешнее -0.394). Брутто при этом растёт
# (+0.008), но у валютных пар сделка стоит 0.37-0.42 R, и дальняя цель добавляет стопов
# быстрее, чем прибыли. Владелец знает и решил так. И главное: брутто у крипты +0.005 —
# это ноль в пределах ошибки, прибыльной стратегия не стала.
JUNE_STOP_ATR = 1.0     # запас стопа за фитилём свечи, в долях ATR
# Минимальная цель в долях СТАРОГО риска, по классам инструментов (instruments.asset_class).
JUNE_MIN_TP_R = {"crypto": 1.0, "fx": 0.5, "commodity": 0.5}

# ОТБОР ПО СИЛЕ УРОВНЯ — ВЫКЛЮЧЕН 22 сентября 2026 (решение владельца). Сильный —
# это часовой уровень, совпавший с дневным в пределах 0.1% (LEVEL_MATCH_TOL), либо
# сам дневной уровень; сейчас сила снова только помечает сигнал приоритетом «high»
# и на отбор не влияет, как было в июньских правилах. Механизм цел: True возвращает
# отбор обратно, не трогая ничего больше.
#
# ПОЧЕМУ ВЫКЛЮЧЕН: движок замолчал. Отбор ушёл в бой 18 сентября в 13:50 (+05) —
# вместе с коммитом про ICT, — и последний сигнал ложного пробоя в боевой базе
# датирован тем же днём: ETH, свеча 18.09 13:00 UTC. Четверо суток тишины на всех
# 21 инструменте. Это не сбой, а ровно объявленная цена правила (см. ниже), но
# владелец частоту ценит выше и решил вернуть её. Проверка 22 сентября прогоном
# БОЕВОГО детектора бар за баром по последним 720 часам (уровни пересчитывались на
# каждом баре, как это делает run_analysis): 11 сигналов на все 21 инструмент против
# 777 без отбора по силе — правило снимало 98.6% сетапов.
#
# ВЫБОР УРОВНЯ (самый глубокий из проколотых) НЕ ТРОНУТ и работает всегда. Сам по
# себе он не меняет ни одной сделки — стоп стоит за фитилём свечи, цель ищется от
# закрытия, — но при возврате отбора по силе он снова становится осмысленным:
# сильный надо искать среди ВСЕХ проколотых, а не только среди первого.
#
# ЗАМЕР june_vol.py, ради которого отбор и вводили 17 сентября 2026 (13 монет +
# GOLD, 27 мес., тот же якорь 12777 сигналов):
#   силу не смотрим (было)            469 сигн./мес  R:R 1.16  брутто +0.004  нетто -0.086
#   первый проколотый — сильный        10            R:R 2.53  брутто +0.134  нетто +0.048
#   ЕСТЬ СИЛЬНЫЙ СРЕДИ ПРОКОЛОТЫХ      16            R:R 2.14  брутто +0.099  нетто +0.020
#   самый глубокий прокол — сильный    12            R:R 2.30  брутто +0.099  нетто +0.016
# Это ПЕРВОЕ положительное нетто за всю историю замеров проекта. Обе половины
# истории в плюс, экзамен после 01.02.2026 тоже (+0.24...+0.30). Механизм виден в
# колонке R:R: от сильного уровня до следующего встречного места вдвое больше.
#
# ЦЕНА ВЫКЛЮЧЕНИЯ — ЭТА ЖЕ ТАБЛИЦА, ЧИТАННАЯ СПРАВА НАЛЕВО: нетто с +0.020
# возвращается на -0.086 R на сделку, R:R с 2.14 на 1.16. То есть отменяется
# ПЕРВОЕ И ЕДИНСТВЕННОЕ положительное нетто за всю историю замеров проекта.
# В защиту решения: преимущество отбора так и не было доказано (1.6 своей ошибки
# при пороге 2 — из пяти пунктов критерия проекта пройдено четыре), а платой была
# почти полная тишина: 469 сигналов в месяц превращались в 16 на все 14
# инструментов, у золота с нефтью — 4 в месяц на двоих.
JUNE_STRONG_ONLY = False

# ── Стратегия №5 — ПРОБОЙ СИЛЬНОГО УРОВНЯ (17 сентября 2026) ────────────────
# Задание владельца: «свеча закрылась выше сильного уровня — сигнал в лонг с расчётом
# риск-прибыль». Занимает место стратегии №4 (тренд), снятой с боя в тот же день.
#
# В БОЮ С 22 сентября 2026 (BREAKOUT_SIGNALS = True, решение владельца) — но не по
# всему движку: сигналы уходят подписчикам только по КРИПТЕ, ЗОЛОТУ и НЕФТИ
# (BREAKOUT_SIGNAL_CLASSES). Валютные пары остаются в слежке молча — считаются и
# ведутся в базу, но никому не шлются.
#
# ПОЧЕМУ ВАЛЮТА ОТДЕЛЬНО: её не было в замере вовсе (13 монет + GOLD + BRENT), то
# есть по ней у стратегии нет ни одной проверенной цифры. Четверо суток слежки это
# подтвердили с той стороны, с какой не хотелось бы: 11 сделок, все шорты, винрейт
# 18%, итог -4.94 R против +27.43 R на крипте. Но 11 сделок — не довод, довод —
# отсутствие замера; поэтому валюта не выключена, а продолжает копить статистику.
#
# СЛЕЖКА ЗА ЧЕТВЕРО СУТОК (18-22 сентября, всё ещё без издержек): 46 сигналов,
# 38 закрыто, +22.5 R, средняя сделка +0.592 ±0.245 R, винрейт 53%, R:R плановый
# 2.35 (медиана 2.0). Это ЛУЧШЕ замера (+0.135 брутто), но разница меньше двух
# своих ошибок, весь плюс сделан крипто-лонгами на растущем рынке, и ни одна сделка
# ещё не истекла по сроку 120 ч. Частота при этом втрое выше замеренной: 16 сигналов
# на инструмент в месяц против 5.5 — расхождение не объяснено, сверку с лабораторией
# бар в бар (как ict_verify.py для стратегии №6) ещё предстоит сделать.
#
# Сводка — /breakout, подписка — /subscribe, метка 📈.
#
# ЗАМЕР june_break.py (13 монет + GOLD + BRENT, 27 мес., сильный уровень, свежий
# пробой, стоп фитиль + 1 ATR):
#   цель 1 риск,  горизонт 48 ч   82 сигн./мес  брутто +0.070  нетто -0.014  экз. -0.069
#   цель 2 риска, горизонт 120 ч  82            брутто +0.135  нетто +0.039  экз. -0.054
#   цель 3 риска, горизонт 120 ч  82            брутто +0.107  нетто +0.010  экз. -0.115
#   контроль: любой уровень       2359          брутто -0.006  нетто -0.102  экз. -0.021
# Сила уровня — весь эффект целиком: на слабых уровнях брутто РОВНО ноль при 64 тыс.
# сделок. Цель 2 риска — внутренний оптимум сетки, а не её край.
#
# ПРОЙДЕНО 5 ПУНКТОВ КРИТЕРИЯ ИЗ 6: брутто положительный и выше двух своих ошибок,
# сделок 2229, обе половины истории в плюс, винрейт выше «монетки» (44% против 39%).
# ПРОВАЛЕН ЭКЗАМЕН: после 01.02.2026 брутто отрицательный. Зеркальная сделка (возврат
# за уровень) ведёт себя ровно наоборот — на старой половине ноль, на свежей +0.208.
# Вместе они гасят друг друга, и это похоже на смену режима рынка, а не на эдж.
# Ради этого слежка и заведена.
BREAKOUT_SIGNALS = True    # False возвращает стратегию в молчаливую слежку
# Классы инструментов, по которым сигналы РАССЫЛАЮТСЯ (instruments.asset_class).
# Всё остальное — крутится в слежке молча. Добавить "fx" сюда = выкатить валюту.
BREAKOUT_SIGNAL_CLASSES = ("crypto", "commodity")
BREAKOUT_STOP_ATR = 1.0    # стоп за фитилём свечи пробоя, в долях ATR
BREAKOUT_MIN_TP_R = 2.0    # цель — встречный уровень не ближе стольких рисков
BREAKOUT_EXPIRE_HOURS = 120  # горизонт сделки: в замере 120 ч лучше 48 на всех целях

# ── Стратегия №6 — ICT: СВИП ЛИКВИДНОСТИ И РАЗРЫВ (18 сентября 2026) ─────────
# Решение владельца: поставить в бой вместо стратегии №4 (тренд, снят 17.09.2026) и
# назвать подписку «ICT». Правила и их разбор — в докстринге ict.py.
#
# НА ЧАСОВЫХ СВЕЧАХ С 19.09.2026. Первый день стратегия работала на M15 — владелец
# перенёс её на часовик. Правила НЕ менялись ни на одну строку: поменялись два числа,
# ICT_TIMEFRAME и ICT_TF_MINUTES (третье, ICT_CANDLE_LIMIT, следует из них). Обратно
# на M15 — тем же способом: "15m" / 15 / 960.
#
# ЗАМЕР (ict.py / ict_drift.py, 17–18.09.2026, 14 инструментов; H1 за 833 дня, M15 за
# 100 дней; вход по рынку, цель не ближе одного риска, горизонт 48 ч):
#   ЧАСОВИК (в бою с 19.09)  16628 сдел  611/мес  брутто +0.019  цена 0.050  нетто -0.031
#   M15 (был в бою день)      7731 сдел 2480/мес  брутто -0.035  цена 0.125  нетто -0.160
#   канон школы на M15         379 сдел  122/мес  брутто -0.274  цена 0.188  нетто -0.462
#
# ЧАСОВИК ЛУЧШЕ ПО ВСЕМ КОЛОНКАМ, и механизм известен: стоп шире (2.72% цены против
# 1.03%), а комиссия берётся в процентах ЦЕНЫ и делится на риск — сделка дешевеет
# втрое. Девятое подряд воспроизведение закона «издержки в R», здесь в нашу пользу.
#
# ЦЕНА ПЕРЕНОСА НАЗВАНА ДО ПРАВКИ — ЧАСТОТА ПАДАЕТ ВЧЕТВЕРО: 611 сигналов в месяц на
# 14 инструментов против 2480, то есть около 1.5 в день на инструмент вместо шести.
#
# ТРИ ВЕЩИ, КОТОРЫЕ ЗАМЕР СКАЗАЛ ПРЯМО, И ИХ НАДО ПОМНИТЬ:
#   • свип ликвидности НЕОТЛИЧИМ ОТ СЛУЧАЙНОЙ ЛИНИИ. 25959 реальных событий против
#     30683 плацебо (расстояние до уровня перемешано между событиями): разница сноса
#     -0.006 ±0.023. Разрыв БЕЗ свипа ведёт себя не хуже — снос +0.006 против -0.021;
#   • ни одно требование модели не несёт нагрузки: со сломом структуры +0.020, без
#     него +0.020, без порога импульса столько же (H1, полная выборка);
#   • ЭКЗАМЕН ОТРИЦАТЕЛЬНЫЙ: на часовике брутто +0.033 до 01.02.2026 и -0.017 после.
#     Прибыльной стратегия не является ни в одном варианте.
ICT_SIGNALS = True          # False — бот перестаёт искать и слать сигналы ICT
ICT_TIMEFRAME = "1h"
ICT_TF_MINUTES = 60         # длина свечи в минутах — для пересчёта срока жизни пула
# Свечей за запрос: ICT_POOL_AGE_H (240 ч) плюс запас на разогрев ATR и свингов.
# Значение намеренно равно TREND_H1_LIMIT: запрос идёт в тот же ключ кеша, что у
# стратегии №4 и у направления ложного пробоя, и лишнего похода на биржу не случается.
# На M15 здесь стояло 960 (10 суток — больше BingX за один раз не отдаёт), и срок
# жизни пула замерен на обоих: 720 ч против 240 — брутто -0.033 против -0.037 при
# ошибке 0.013, то есть разницы нет.
ICT_CANDLE_LIMIT = 400
ICT_POOL_W = 2              # окно свинга-пула ликвидности
ICT_STRUCT_W = 1            # окно свинга для слома структуры — канон школы
ICT_EQ_ATR = 0.10           # равные экстремумы — один пул (как _pools ложного пробоя)
ICT_POOL_AGE_H = 240        # сколько часов пул считается живым
ICT_SWEEP_BARS = 3          # сколько свечей после свипа даётся импульсу
ICT_DISP_ATR = 0.50         # тело импульсной свечи, в долях ATR
ICT_FVG_ATR = 0.05          # ширина разрыва, в долях ATR
ICT_STOP_ATR = 0.10         # запас стопа за экстремумом манипуляции, в долях ATR
ICT_MIN_TP_R = 1.0          # цель — ликвидность не ближе стольких рисков
ICT_FALLBACK_RR = 2.0       # нет подходящей ликвидности — цель ровно 2 риска
ICT_EXPIRE_HOURS = 48       # горизонт сделки, часов — как в замере
# Не чаще одного сигнала на инструмент и сторону. На часовике 90 минут означают «не
# два часа подряд» — ровно как SIGNAL_DEDUP_MIN у ложного пробоя, и ровно так стояло
# в замере (серия соседних свечей одного импульса иначе шла бы отдельными сигналами).
ICT_DEDUP_MIN = 90

TREND_TF_HOURS = 6      # свечи, по которым движок ложного пробоя смотрит направление
# Сколько часовых свечей тянуть под это направление. 400 часов = 66 шестичасовых свечей,
# EMA20 есть на чём считать. Значение намеренно равно TREND_H1_LIMIT: запрос идёт в тот
# же ключ кеша, что у стратегии №4, и лишнего похода на биржу не случается.
TREND_TF_H1_LIMIT = 400

# Стратегии, по которым подписываются в /subscribe (15 сентября 2026). Подписка — пара
# «инструмент + стратегия»: одну монету можно вести по одной стратегии, другую по другой.
# Тренд (стратегия №4) убран отсюда 18.09.2026 вместе с выходом ICT в бой: он снят
# с боя 17.09.2026, и держать в меню галочку стратегии, которая не даёт сигналов,
# значило бы обещать несуществующее. Код тренда, его тесты и команда /trend целы —
# возврат в подписку это одна строка здесь плюс обратная миграция.
STRATEGIES = {
    "spring": "Ложный пробой",
    "ict": "ICT — свип ликвидности",
    "breakout": "Пробой уровня",
}

# ── Кеш свечей (в секундах) — чтобы мониторинг не дёргал биржу зря ───────────
# 1m — для алертов «касание уровня»: минутные свечи живут недолго, а проверка
# идёт раз в 5 минут, поэтому длинный TTL отдал бы протухшие данные.
# 15m — свечи движка при ROLLING_HOUR: TTL короче интервала мониторинга, иначе
# скользящий час обновлялся бы реже, чем собирается, и весь выигрыш во времени
# ушёл бы в кеш.
CACHE_TTL = {"1m": 60, "15m": 120, "1h": 300, "1d": 3600}

# ── Аналитика (тренд, уровни, ликвидность) ──────────────────────────────────
EMA_PERIOD = 20         # период EMA для определения глобального тренда
TREND_BAND = 0.001      # мёртвая зона ±0.1% вокруг EMA → 'sideways'
D1_PIVOT_WINDOW = 5     # окно поиска пиков/впадин на D1 (глобальные уровни)
H1_PIVOT_WINDOW = 3     # окно поиска пиков/впадин на H1 (локальные уровни)
LEVEL_MATCH_TOL = 0.001  # совпадение уровня H1 с D1 в пределах 0.1% → 'strong'

# ── Паттерны (Spring / Upthrust) ────────────────────────────────────────────
# ОКНО СРЕДНЕГО ОБЪЁМА — 12 ЧАСОВ (17 сентября 2026, просьба владельца «считай
# среднее за 12, поэкспериментируй 5-12»). Было 20.
#
# ЗАМЕР june_vol.py (13 монет + GOLD + BRENT, 27 мес., якорь воспроизвёл боевые
# 12777 сигналов и брутто +0.004). Сетка взята шире просьбы, 5...24 часа:
#   5 ч  503 сигн./мес  брутто -0.003      14 ч  486 сигн./мес  брутто +0.002
#   8 ч  502            брутто -0.005      16 ч  481            брутто +0.004
#  10 ч  497            брутто +0.003      20 ч  469 (было)     брутто +0.004
#  12 ч  490            брутто +0.002      24 ч  446            брутто +0.005
# ВЕСЬ ДИАПАЗОН ЛЕЖИТ ВНУТРИ ОДНОЙ ОШИБКИ (±0.008): окно среднего объёма на
# качество сделки не влияет, дуги доза-эффект нет. Меняется только частота — на
# коротком окне всплеск засчитывается чаще. Цена решения поэтому нулевая: 490
# сигналов в месяц вместо 469 при том же качестве.
VOL_LOOKBACK = 12       # сколько свечей берём для среднего объёма
SIGNAL_DEDUP_MIN = 90   # не дублировать одинаковый сигнал чаще, чем раз в N минут

# ── Пороги отбора сигнала (константы, из /settings убраны 20 августа 2026) ──
# Раньше эти числа крутил каждый подписчик кнопками. Убраны из меню не потому, что
# не работают, а потому, что крутить их наугад бессмысленно: все варианты уже
# перебраны на 833 днях истории (см. CLAUDE.md, «Замер 19 августа»), и ни один не
# вывел движок в плюс. Меню теперь про другое — про строгость отбора (два
# ATR-фильтра ниже), где зависимость «доза — эффект» хотя бы измерена.
VOL_MULT = 1.5          # во сколько раз объём свечи > среднего → аномальный.
                        # Процентиль объёма вместо множителя проверен и отвергнут:
                        # брутто растёт, нетто рушится втрое (сделок втрое больше —
                        # и комиссии втрое больше).

# ── Пороги детектора — В ДОЛЯХ ATR, а не в процентах цены (2 сентября 2026) ──
# Раньше глубина пробоя и запас стопа задавались процентом ЦЕНЫ (0.05% и 0.1%), и
# только у форекса — в долях ATR. Мерка стала единой для всех инструментов: «0.05%
# цены» у биткоина и у золота значат разное, а «0.05 ATR» — одно и то же.
#
# ЗНАЧЕНИЯ ВЗЯТЫ ПО ЭКВИВАЛЕНТУ, а не назначены на глаз. Посчитано по кешу
# лаборатории (20000 часовых свечей BingX на инструмент): во сколько ATR обходились
# прежние проценты — медиана по всей истории каждого инструмента.
#
#   инструмент   ATR, % цены   пробой 0.05% в ATR   стоп 0.1% в ATR
#   SUI             1.397%           0.036               0.072
#   AVAX            1.170%           0.043               0.085
#   ADA             1.130%           0.044               0.089
#   XRP             0.903%           0.055               0.111
#   BTC             0.607%           0.082               0.165
#   GOLD            0.389%           0.129               0.257
#   ── медиана по 16 инструментам движка ──   0.049               0.097
#      (движок тогда был без форекса; валютные пары вернулись в него 3 сентября,
#       пороги при этом не пересчитывались — мерка и так общая)
#
# Отсюда 0.05 и 0.10 — ровно медиана набора: «в среднем по движку» отбор остался
# прежним. А перекос по инструментам ушёл, и он был немаленький: ЗОЛОТО требовало
# прокола в 2.6 раза глубже (в своих же ATR), чем SUI, только потому что у него ниже
# волатильность в процентах цены. Теперь порог для них один и тот же.
BREAK_ATR = 0.05        # минимальная глубина ложного пробоя — доля ATR (по эквиваленту)

# ЗАПАС стопа за структурный экстремум. По эквиваленту прежних процентов вышло бы 0.10
# (см. таблицу выше), и сначала стояло именно оно. Заменено на 0.25 по замеру
# stop_struct.py (2 сентября, та же выборка 12752 сигнала — число сделок от стопа не
# зависит, поэтому строки сравнимы напрямую):
#
#   запас       риск, % цены   вин   брутто   цена сделки   нетто
#   0.10 ATR        1.56%      58%   -0.058      0.108      -0.167
#   0.25 ATR        1.72%      61%   -0.050      0.086      -0.136   <- принято
#   0.50 ATR        1.98%      65%   -0.042      0.065      -0.106
#
# Почему не 0.50, хотя по деньгам она лучше: риск сделки уходит к 2% цены при близкой
# цели, и сигнал начинает выглядеть как «рискни двумя процентами, чтобы взять один».
# Решение владельца — середина.
#
# ЧЕСТНО ПРО МЕХАНИЗМ. Улучшение здесь чисто издержечное: комиссия берётся в процентах
# ЦЕНЫ и делится на риск сделки, поэтому широкий стоп дешевле в R. Брутто от ширины
# почти не зависит и отрицателен на всех вариантах. Эджа это не добавляет.
STOP_ATR = 0.25

# За КАКОЙ экстремум ставится стоп: сколько часовых свечей ПЕРЕД свечой свипа берём
# в расчёт вместе с ней. Стоп уходит за ДАЛЬНИЙ из них плюс запас STOP_ATR.
#   0 — только свеча свипа (как было до 2 сентября 2026);
#   1 — свеча свипа и одна перед ней  <- принято.
#
# ЗАЧЕМ. Формулировка владельца: стоп должен стоять за часовым поглощением или за
# часовым фитилём, которым перед ним вынесло по стопам. У нынешнего движка свеча свипа
# и есть тот самый фитиль, а «поглощение» — соседний бар; значит структура сделки это
# ПАРА свечей, и стоп принадлежит паре, а не одной из них.
#
# ИЗМЕРЕНО 2 сентября (stop_struct.py). Правило лучше того стопа, которое заменило:
# нетто -0.190 -> -0.167, винрейт 55% -> 58%, цена сделки 0.123 -> 0.108 R.
#
# НО выигрыш даёт ШИРИНА, а не структура. В том же прогоне рядом считался контроль —
# просто широкий стоп от свечи свипа. При сопоставимой средней ширине (около 1.7% цены)
# контроль идёт чуть ЛУЧШЕ: брутто -0.042 против -0.050, нетто -0.113 против -0.136.
# То есть структура сетапа не несёт информации о будущем; правило полезно тем, что
# подстраивает ширину стопа под размах конкретного сетапа, и только этим.
#
# ВАЖНО ПРО МЕХАНИЗМ, а не про надежду. Широкий стоп удешевляет сделку в R по чистой
# арифметике: издержки берутся в процентах ЦЕНЫ и делятся на риск сделки. Эджа он не
# добавляет — брутто от ширины стопа почти не зависит. То есть это лечение ИЗДЕРЖЕК,
# и выдавать его за прибыльность нельзя.
STOP_STRUCT_BARS = 1

# СИЛА ОТБОЯ: где свеча свипа закрылась внутри собственного размаха. Для лонга 0.6
# значит «закрытие не ниже верхних 40% размаха», для шорта зеркально. Смысл словами:
# прокол должны были ВЫКУПИТЬ решительно, а не еле-еле. Свеча, кое-как переползшая
# уровень обратно, и свеча, вынесшая его импульсом, до 2 сентября 2026 были для
# движка одинаковы — этот порог их различает.
#
# ЭТО ЕДИНСТВЕННЫЙ РЫЧАГ ПРОЕКТА, УЛУЧШИВШИЙ И БРУТТО, И НЕТТО СРАЗУ. Все прежние
# улучшали одно за счёт другого: фильтры строгости поднимали качество отбора и тем же
# движением дорожали в R, широкий стоп удешевлял сделку и не трогал брутто.
#
# ЗАМЕР (edge.py / edge_dose.py / edge_net.py, 2 сентября, 14 инструментов, 20000
# часовых свечей на каждый). Тонкая сетка по семи порогам:
#
#   порог      сигналов/мес   брутто   цена сделки   нетто
#   выкл           466        -0.050      0.086      -0.136
#   >= 0.45        367        -0.037        —          —
#   >= 0.50        348        -0.036      0.060      -0.095
#   >= 0.55        326        -0.033        —          —
#   >= 0.60        302        -0.030      0.055      -0.084   <- принято
#   >= 0.70        248        -0.037        —          —
#
# Улучшение МОНОТОННОЕ до 0.60, обе половины истории идут вместе, дальше разворот.
# Оптимум ВНУТРЕННИЙ, а не на краю сетки, — это собственная проверка проекта на
# «механизм, а не подгонка под диапазон перебора». Первая грубая сетка (0.5/0.66/0.8)
# дала «пик и провал» и едва не привела к неверному выводу; шаг 0.05 показал дугу.
#
# ЗНАЧИМОСТЬ ПРОВЕРЕНА ПРАВИЛЬНЫМ ТЕСТОМ (disp_bos.py, 2 сентября): оставленные
# сделки против ОТБРОШЕННЫХ, а не против нуля. Группы не пересекаются, сравнение
# честное: +3.3 своих ошибки. Прежний t против нуля отвечал на вопрос «надёжно ли мы
# в минусе» и для отбора рычага не годился.
#
# ЦЕНА РЕШЕНИЯ — ЧАСТОТА: 466 -> 302 сигнала в месяц, то есть треть уходит.
#
# ЧЕГО ЭТО НЕ ДЕЛАЕТ. Брутто остаётся ОТРИЦАТЕЛЬНЫМ (-0.030 вместо -0.050 — сорок
# процентов пути до нуля). Прибыльной стратегию порог не делает и не обещает.
# Продолжения у рычага нет: displacement (тот же импульс, но в долях ATR) и BOS
# измерены 2 сентября и отклонены — см. CLAUDE.md, «DISPLACEMENT и BOS».
MIN_CLOSE_POS = 0.6

# ── СВИП ОБЯЗАН БЫТЬ СВИПОМ (5 сентября 2026) ───────────────────────────────
# Сколько свечей ПЕРЕД свечой свипа проверяем на то, что цена была по нужную
# сторону уровня: для лонга хотя бы одна из них должна закрыться ВЫШЕ поддержки.
#
# ЗАЧЕМ. До этой правки детектор спрашивал только «ушёл ли минимум под уровень» и
# «закрылись ли обратно над ним» — и не спрашивал, была ли цена над уровнем ДО
# этого. Любая свеча, выкупившая уровень СНИЗУ, читалась как пружина, хотя
# ликвидность там никто не снимал: цена просто вернулась туда, откуда раньше упала.
#
# Два живых примера, на которых это поймано (оба разобраны в CLAUDE.md):
#   GOLD 2 сентября 11:00 UTC — «поддержкой» служил минимум часовой свечи от
#     1 сентября, то есть уровень СВЕРХУ. Цена пять часов подряд закрывалась под
#     ним (4326 → 4314), потом свеча прошла его вверх и закрылась на 4337.30.
#     Движок выдал сигнал. Свипа не было вообще;
#   BTC 6 июля 15:00 UTC — то же самое, уровень 62376.20, свеча открылась под ним
#     на 62019.90. Стоп получился 61575.82, то есть риск 3% цены при обычных 0.5%.
#
# Это дефект ОПРЕДЕЛЕНИЯ СОБЫТИЯ, а не настройка. Ровно такую ошибку проект уже
# ловил 21 августа в лабораторном momentum.py («детектор считал пробоем любой
# уровень ниже закрытия») и чинил там тем же требованием свежего пересечения;
# в боевой Spring исправление тогда не перенесли.
#
# ПОЧЕМУ ИМЕННО ТРИ, А НЕ ОДНА. Одной свечи мало: сетап бывает двухсвечным —
# свеча А уходит под уровень и закрывается ПОД ним, свеча B выкупает обратно
# (см. SWEEP_WINDOW). Если смотреть только на соседа, у такого сетапа соседом
# окажется сама А, и он отбракуется вместе с браком. Три свечи назад — это окно,
# в которое двухсвечный свип помещается целиком, а «цена давно живёт под уровнем»
# уже не помещается: в обоих примерах выше цена была под уровнем 5+ часов.
#
# ЗАМЕРОМ НЕ ПРОВЕРЕНО. Правка сделана по разбору живых сигналов, а не по прогону:
# это починка ошибки, а не подбор порога. Сколько сигналов она отнимет и что
# сделает с брутто — не измерено; мерить это надо тем же rr_off.py / filters_new.py.
FRESH_CROSS_BARS = 3

# СКОЛЬКО СВЕЧЕЙ НАЗАД ОТ СИГНАЛЬНОЙ РАЗРЕШЕНО СТОЯТЬ СВЕЧЕ СВИПА.
# 0 (боевое) — одна свеча обязана и проколоть уровень, и закрыться обратно, и
# объём меряется на НЕЙ. Это «аномальный объём на свече пробоя» — правило
# первого движка (22 июня 2026) и всех замеров проекта.
#
# ВВЕДЕНО 5 сентября 2026 значением 1, ОТКАЧЕНО 7 сентября — решение владельца.
# Механизм оставлен целиком: константа его включает обратно, тест этого режима
# на месте. Ниже обе стороны решения, чтобы следующая правка знала цену.
#
# ЗАЧЕМ ВВОДИЛИ. Рынок снимает ликвидность не по часам. Типичная
# последовательность: свеча А выносит уровень длинной тенью на объёме и
# закрывается ПОД ним, свеча B выкупает её телом обратно. Глазами это один
# сетап: у А закрытие не вернулось за уровень, у B минимум уже не заходит под
# него, и по отдельности условий не проходит ни одна.
# Пример (GOLD 2 сентября, M15): 03:30 — свип до 4287.20, объём ×1.81, закрытие
# 4294.49 под пулом; 03:45 — поглощение телом втрое крупнее, закрытие 4308.49,
# отбой 0.97, а собственный объём всего ×0.82.
# При SWEEP_WINDOW=1 от свечи B дополнительно требуется ПОГЛОЩЕНИЕ тела А
# (_engulfs): без него «две свечи» означали бы «любой возврат за уровень в
# пределах пары часов», а это уже не свип.
#
# ПОЧЕМУ ОТКАТИЛИ. Три довода, все проверяемые:
#   • ТОТ ПРИМЕР ЭТИМ ВСЁ РАВНО НЕ ЛОВИТСЯ. Он снят на M15, а движок работает по
#     часовой свече, и на ней всплеск размазан: ×1.1 при пороге ×1.5. Это
#     записано и в CLAUDE.md, в разделе «что это НЕ починило». Значит в бою
#     правило берёт какие-то ДРУГИЕ сетапы, и какие — никто не смотрел;
#   • ЭТО ОСЛАБЛЕНИЕ ФИЛЬТРА ОБЪЁМА, а не усиление: у объёма появлялся второй
#     шанс пройти (сигнальная свеча ИЛИ поглощённая ею предыдущая);
#   • ВОЗВРАТ — К ИЗМЕРЕННОМУ. Все опубликованные числа проекта (302 сигнала в
#     месяц, брутто −0.016, нетто −0.077, подписи кнопок /settings) сняты с
#     объёмом на свече пробоя; лабораторный детектор устроен так же. А сам
#     двухсвечный свип не измерялся вовсе — его вводили по разбору живых
#     сигналов.
#
# ЧТО ПОТЕРЯНО И НЕ ИЗМЕРЕНО: сколько двухсвечных сетапов на ЧАСОВОЙ свече
# правило давало и что они приносили. Мерить это надо переносом правила в
# лабораторию (там его нет) и прогоном rr_off.py / filters_new.py на 833 днях.
SWEEP_WINDOW = 0

# ── ЛОВУШКА, РАСТЯНУТАЯ НА НЕСКОЛЬКО ЧАСОВ (7 сентября 2026) ────────────────
# Сколько часов движок ПОМНИТ прокол, который в свою свечу обратно не вернулся.
# 0 — прежнее поведение: прокол и возврат обязаны уложиться в одну свечу.
#
# ТЗ ВЛАДЕЛЬЦА, по пунктам: цена подходит к уровню → прокалывает и выходит за него
# (это ещё НЕ сигнал, только внутреннее состояние SETUP) → ждём возврата обратно за
# уровень → проверяем, что возврат решительный и на аномальном объёме → и только
# теперь сигнал. Первый прокол пользователю не отправляется никогда.
#
# ЧЕМ ЭТО НЕ ЯВЛЯЕТСЯ. Это НЕ отложенный вход и НЕ ожидание подтверждения: сигнал
# уходит по закрытию свечи ВОЗВРАТА, то есть в момент, когда ловушка захлопнулась,
# и ждать после него нечего. Отложенный вход в проекте измерен четырежды и
# отклонён (volume_retest.py, disp_bos.py, m15_entry.py, m15_signal.py) — там
# ждали ПОСЛЕ готового сетапа, и близкая цель уходила без нас. Здесь ровно
# наоборот: раньше сетапа не было вовсе.
#
# ЧЕМ ЭТО НЕ ЯВЛЯЕТСЯ (2): это не возврат SWEEP_WINDOW. Тот мерил объём на свече
# ПРОКОЛА и требовал от свечи возврата поглотить её тело. Здесь объём и сила отбоя
# проверяются на СИГНАЛЬНОЙ свече — той, что вернула цену за уровень. Это прямое
# требование владельца и, кстати, ровно правило первого движка: аномальный объём
# на той свече, по которой принимается решение.
#
# КАК ЭТО РАБОТАЕТ НА САМОМ ДЕЛЕ — важнее, чем звучит формулировка. Свеча возврата
# ВСЕГДА открывается там, где закрылась предыдущая, то есть за уровнем, и потому
# сама его прокалывает. Значит первый путь детектора её и так видит. Реальный
# эффект рычага в другом: он переносит проверку СВЕЖЕСТИ свипа (_fresh_cross) со
# свечи возврата на свечу ПРОКОЛА. Для свечи возврата предыдущие часы все за
# уровнем, и свежесть не проходит; от свечи прокола цена ещё стояла по нужную
# сторону — и сетап оживает. Проще говоря, окно свежести удлиняется на время,
# пока ловушка захлопывается. Поймано тестом, а не рассуждением: первая версия
# фикстуры сработала не той веткой.
#
# ЗАМЕР (spring_state.py, 7 сентября 2026; BTC 833 дня + GOLD 309 + BRENT 166,
# издержки «лимитом», исход по H1, горизонт 48ч):
#
#   вариант                сдел    /мес   вин   брутто   цена сделки   нетто
#   всё в одной свече      1576      58   58%   -0.034      0.165      -0.199
#   ждём возврата до 1 ч   1640      60   59%   -0.028      0.163      -0.191
#   ждём возврата до 2 ч   1763      64   59%   -0.034      0.158      -0.192
#   ждём возврата до 3 ч   1911      70   59%   -0.033      0.151      -0.184
#
# ГЛАВНАЯ СТРОКА ЗАМЕРА — какими оказались ТОЛЬКО ДОБАВЛЕННЫЕ сетапы: их 341, и их
# брутто -0.038 против -0.034 у якоря при ошибке ±0.040. То есть сетапы, которых
# движок раньше не видел, ровно ТАКИЕ ЖЕ, как те, что он брал, — не лучше и не
# хуже. Критерий, объявленный до прогона (сигналов не меньше +20%, добавленные не
# хуже якоря, минимум на двух инструментах), пройден: +21%, и нетто улучшилось на
# ВСЕХ трёх инструментах.
#
# ОТКУДА УЛУЧШЕНИЕ НЕТТО, если брутто не изменилось: цена сделки падает
# 0.165 -> 0.151 R. Стоп теперь охватывает весь сетап, от прокола до возврата,
# значит риск шире, а комиссия берётся в процентах ЦЕНЫ и делится на риск. Это
# знакомая арифметика «издержек в R», в кои-то веки в нашу пользу.
#
# ЧЕМ ЭТОТ РЫЧАГ ОТЛИЧАЕТСЯ ОТ ВСЕХ ПРЕЖНИХ: он ДОБАВЛЯЕТ сигналы, а не режет.
# Сила отбоя стоила трети сигналов, фильтры /settings режут в разы. Этот даёт
# +21% и не портит среднюю сделку.
#
# ЧЕГО ЭТО НЕ ДЕЛАЕТ: брутто как было отрицательным, так и осталось (-0.033
# против -0.034). Прибыльной стратегия не стала, и эджа этот рычаг не добавил —
# он добавил ОХВАТ по той же цене.
#
# ГРАБЛИ ЗАМЕРА, записаны, чтобы не повторить: первый прогон шёл собственной
# копией машины состояний рядом с боевым детектором — и не проверял свежесть
# свипа, которой в коде есть. Числа вышли завышенными (+22% сетапов с брутто
# -0.016). После того как правило вшили в детектор, копия стала дублировать его,
# и замер напечатал «добавлено 0», чем себя же и поймал. Правильный способ —
# ПОДМЕНА КОНСТАНТЫ на боевом детекторе, так замер и переписан.
#
# 3 выбрано по критерию: только этот порог даёт заявленные +20% охвата. При 1 и 2
# прибавка 4% и 12%, то есть рычаг почти не работает.
SWEEP_WAIT_BARS = 3

# ── ПУЛ ЛИКВИДНОСТИ: РАВНЫЕ ЭКСТРЕМУМЫ (5 сентября 2026) ────────────────────
# Допуск, в пределах которого два минимума (или два максимума) считаются РАВНЫМИ и
# образуют пул ликвидности — в долях ATR. За такими равными краями и стоят чужие
# стопы; именно их и выносит свип.
#
# ЗАЧЕМ. Уровни движка — фракталы с окном config.H1_PIVOT_WINDOW (3), а фрактал
# подтверждается только через три свечи СПРАВА. То есть уровень появляется в базе
# через три часа после того, как он сформировался, — а свип случается раньше.
#
# GOLD 2 сентября: двойное дно 4293.57 (01:00) и 4293.53 (02:00), свип в 03:00 с
# минимумом 4287.20 и полным выкупом (отбой 0.92). В списке уровней движка на тот
# момент ближайшая поддержка была 4421.50 — на сто с лишним долларов выше цены.
# Свипать было нечего. Уровень 4287.20 появился только к 05:00, через два часа
# после события. Пул из равных минимумов виден СРАЗУ, правого окна ему не нужно.
#
# ВАЖНО: фрактал ТУТ ВООБЩЕ НЕ ПОМОГ БЫ даже с окном 1 — минимум 02:00 не является
# локальным, потому что 03:00 ушёл ниже. Равные экстремумы это отдельная сущность,
# а не фрактал с более коротким окном.
#
# 0.1 ATR — это «одна десятая обычного часового хода»: у золота при ATR ≈ 19.5 то
# есть допуск около двух долларов, у пары 4293.57/4293.53 расхождение 0.04. Число
# назначено, а не подобрано замером; POOL_MIN_TOUCHES = 2 — минимальное, при котором
# слово «равные» вообще имеет смысл.
EQUAL_EXTREME_ATR = 0.1
POOL_LOOKBACK = 24      # в скольких свечах перед свипом ищем равные экстремумы
POOL_MIN_TOUCHES = 2    # сколько касаний делают пул пулом

# ФОРЕКС ВЕРНУЛСЯ В ДВИЖОК 3 сентября 2026 (решение владельца, без замера). Пары
# NCFX* были в движке с 21 августа по 2 сентября, потом их вывели по деньгам, теперь
# вернули на новый движок — снятый фильтр R:R, пороги в ATR, структурный стоп, сила
# отбоя, скользящий час. Состав движка решает instruments.engine_codes(), и условие
# там снова одно: есть биржевой источник свечей.
#
# ОТДЕЛЬНЫХ ПОРОГОВ У ФОРЕКСА НЕТ И НЕ ВЕРНУЛОСЬ. Константы FX_BREAK_ATR /
# FX_STOP_ATR / FX_IGNORE_ATR_FILTERS ушли 2 сентября вместе с процентами цены: они
# существовали ровно затем, чтобы задать валютным парам пороги в ATR, пока у всех
# остальных пороги были в процентах. Теперь в ATR считают все — и BREAK_ATR, и
# STOP_ATR, и оба фильтра /settings действуют на валютные пары так же, как на крипту.
#
# ЦЕНА РЕШЕНИЯ НАЗВАНА ЗАРАНЕЕ И ЗАМЕРОМ НЕ ОТМЕНЕНА: 20 августа форекс дал -1.385 R
# на сделку, из которых 1.153 R — издержки (у него риск сделки в процентах цены мал,
# а комиссия в процентах цены та же, что у крипты, поэтому та же сделка стоит в R на
# порядок дороже). Ни одна из 21 проверенной конфигурации не вышла в плюс. На нынешнем
# движке форекс НЕ мерился вовсе. См. CLAUDE.md, «Попытка вылечить форекс».
LIQUIDITY_MULT = 1.5    # объём зоны ликвидности > среднего × этого.
                        # Влияет ТОЛЬКО на отображение зон в /analyze, не на отбор.
FALLBACK_RR = 2.0       # ЗАПАСНАЯ ЦЕЛЬ, когда впереди нет ни одного уровня: цель
                        # ставится на FALLBACK_RR x риск. Это НЕ фильтр.
                        #
                        # Фильтром это было до 2 сентября 2026 (константа MIN_RR):
                        # сигнал отбрасывался, если ближайший встречный уровень давал
                        # меньше 1:2. ОТБРАКОВКА СНЯТА по решению владельца — она
                        # срезала около 80% сетапов, и движок почти всё время молчал.
                        # Цель теперь ставится на ближайший встречный уровень, какой
                        # есть, — ровно как в первом движке (коммит c855660, 22 июня).
                        #
                        # Сама константа осталась, потому что делала ДВЕ вещи, и вторая
                        # нужна по-прежнему: без неё у сигналов, перед которыми уровня
                        # нет вовсе, не было бы цели.
                        #
                        # ЧЕСТНО ПРО ЦЕНУ РЕШЕНИЯ — замер первого движка (21 августа,
                        # ../iron-wake-lab/first_engine.py, копия того самого коммита):
                        # без фильтра R:R сигналов впятеро-вшестеро больше при почти той
                        # же цене сделки в R, то есть убыток за период растёт во столько
                        # же раз. Винрейт при этом РАСТЁТ (23% -> 57%), потому что цель
                        # становится ближе, — но это не улучшение, а другая форма той же
                        # отрицательной математики.

# МИНИМАЛЬНОЕ РАССТОЯНИЕ ДО ЦЕЛИ в долях ATR (3 сентября 2026). При выборе цели
# уровни ближе этого расстояния от закрытия ПРОПУСКАЮТСЯ, берётся следующий за ними.
#
# Откуда взялось: живой сигнал по золоту 2 сентября — вход 4337.30, стоп 4302.09,
# цель 4339.29. Риск 35 долларов ради 2. Сделка выиграла, но золото в тот же день
# сходило на 4401, то есть мимо прошло 64. Такие цели давал не сбой, а правило
# «ближайший уровень, какой есть» — прямая цена снятия фильтра R:R (см. FALLBACK_RR).
# И это не редкость: у ТРЕТИ сделок цель ближе 0.2 риска, у каждой пятой — ближе 0.1.
#
# ЧЕМ ЭТО ОТЛИЧАЕТСЯ ОТ ВОЗВРАТА ФИЛЬТРА R:R — тем, что здесь ничего не отбрасывается.
# Тот резал СИГНАЛ (около 80% сетапов), этот двигает ЦЕЛЬ. Число сигналов не меняется
# вовсе: 302 в месяц и до, и после. Владелец ценит частоту — она не тронута.
#
# Замер: ../iron-wake-lab/target_min.py, 14 инструментов, 20000 часовых свечей,
# 8275 сделок. Якорь воспроизвёл боевую конфигурацию точно (-0.030 брутто, -0.084
# нетто). Выборки ПАРНЫЕ — те же сделки с другой геометрией, поэтому разница считана
# по сделкам попарно, а не как разность двух средних (это чувствительнее в разы).
#
#   порог      вин    R:R    брутто    нетто    разница к базе
#   нет        69%   0.61    -0.030   -0.084          —
#   0.35 ATR   62%   0.76    -0.020   -0.079      +0.010 (t 2.4)
#   0.45 ATR   61%   0.81    -0.017   -0.077      +0.013 (t 2.8)
#   0.50 ATR   60%   0.83    -0.016   -0.077      +0.013 (t 2.7)
#   0.65 ATR   57%   0.92    -0.017   -0.080      +0.013 (t 2.2)
#   1.10 ATR   50%   1.21    -0.020   -0.089      +0.010 (t 1.3)
#
# Критерий, объявленный ДО прогона, прошли СЕМЬ порогов подряд (0.35...0.65) —
# связный диапазон, а не одиночная точка, и оптимум по деньгам внутренний (0.45-0.50),
# а не на краю сетки. Поэтому 0.5: середина плато, угадывать точное число не нужно.
# Эффект размазан по набору, а не держится на паре инструментов: улучшилось 10 из 14,
# крупнее всего у самых ликвидных (GOLD +0.041, BTC +0.027).
#
# ЧЕГО ЭТО НЕ ДЕЛАЕТ. Брутто вдвое лучше, но остаётся отрицательным, а НЕТТО сдвинулось
# всего на 8% (-0.084 -> -0.077): при дальней цели чаще выходишь по стопу, а стоп это
# тейкер, он дороже мейкерского выхода по цели — цена сделки растёт 0.055 -> 0.061.
# Прибыльной стратегия не стала. Винрейт падает 69% -> 60%, и это арифметика цели,
# а не ухудшение: дальняя цель доходит реже.
MIN_TARGET_ATR = 0.5

# Период ATR (среднего истинного диапазона) — мера волатильности инструмента.
# С 2 сентября 2026 ATR нужен ДЕТЕКТОРУ ВСЕГДА: в нём заданы и глубина пробоя, и
# запас стопа (BREAK_ATR / STOP_ATR выше). Раньше он требовался только двум фильтрам
# строгости и отчёту /analyze, поэтому и считался лишь когда те включены.
# Почему вообще ATR: расстояние в процентах цены у биткоина и у золота значит разное,
# а в долях ATR — одно и то же. 14 — период, на котором калибровались оба фильтра.
ATR_PERIOD = 14

# ── Лимитный вход (заявка вместо входа по рынку) ─────────────────────────────
# ENTRY_PULLBACK — какую долю пути от закрытия свечи пробоя обратно к пробитому
# уровню ждём, прежде чем входить:
#   0    — заявка по цене закрытия свечи;
#   0.5  — на полпути от закрытия к уровню;
#   1.0  — ровно на уровне.
#
# ЗНАЧЕНИЕ 0 — И ЭТО ВСЁ ЕЩЁ ЛИМИТНАЯ ЗАЯВКА, а не вход по рынку. Заявка ставится по
# цене закрытия, то есть там, где цена и так стоит: роль МЕЙКЕРА сохраняется (0.02%
# против 0.05% у тейкера), просто ждать отката не нужно и заявка исполняется сразу.
#
# БЫЛО 0.5 ДО 29 АВГУСТА 2026. Замер 19 августа выбрал 0.5 честно — но на конфигурации,
# где стоял фильтр R:R 1:2, то есть где цель была ДАЛЕКО. После снятия фильтра цель
# ставится на ближайший встречный уровень, часто совсем рядом, и откат 0.5 сломался:
#
#   конфигурация (14 инстр., 20000 свечей H1)   сделок   не исполнено   брутто    нетто
#   бой c R:R, откат 0.5   (замер 19 авг)         2075       322 (13%)   -0.122   -0.370
#   без R:R, откат 0.5                            7012      5740 (45%)   -0.212   -0.410
#   без R:R, откат 0     <- принято              12752         0  (0%)   -0.067   -0.190
#
# МЕХАНИЗМ — адверс-селекция, усиленная близкой целью. Цена доходит до близкой цели,
# не успев откатиться к заявке на полпути, и мимо проходят В ПЕРВУЮ ОЧЕРЕДЬ ВЫИГРЫШНЫЕ
# сделки. Брутто обваливается втрое (-0.067 -> -0.212), и экономия на цене входа этого
# не окупает: сделка становится ХУЖЕ, чем была в бою до всех правок.
#
# То есть «откат 0.5 лучше» и «откат 0 лучше» — оба верны, каждый для своей
# конфигурации. Замер 19 августа не отменён, он просто про другой движок.
ENTRY_PULLBACK = 0.0
# Сколько часов заявка стоит в стакане. Не исполнилась за это время — снимаем,
# сделки не было (статус сигнала 'expired_unfilled').
#
# При ENTRY_PULLBACK = 0 заявка исполняется практически сразу (она стоит там, где цена),
# так что срок жизни почти никогда не наступает — в замере 0 неисполненных из 12752.
# Константу и механизм оставляем: они страхуют от заявки, повисшей из-за того, что бот
# лежал дольше окна свечей, и понадобятся снова, если откат вернут ненулевым.
ENTRY_WAIT_BARS = 4

# ── Трекинг исхода сигналов (2-я волна) ──────────────────────────────────────
SIGNAL_EXPIRE_HOURS = 48  # если за столько часов сигнал не дошёл ни до цели, ни
                          # до стопа — помечаем 'expired' (исход неизвестен)

# ── Алерты «касание уровня» ──────────────────────────────────────────────────
# Касание ловим ДИАПАЗОНОМ минутных свечей за период между проверками, а не
# одной точкой: если цена сходила к уровню фитилём и вернулась, точка это
# пропустит. Окно берём с запасом (10 минут > 5 минут интервала), чтобы между
# соседними проверками не оставалось дырки.
ALERT_TIMEFRAME = "1m"
ALERT_LOOKBACK = 10     # сколько минутных свечей смотрим за одну проверку

# ── Стакан (DOM, только крипта) ──────────────────────────────────────────────
ORDERBOOK_LIMIT = 50    # сколько уровней стакана тянем с каждой стороны
ORDERBOOK_TTL = 30      # кеш стакана (сек) — стакан меняется быстро, держим коротко
ORDERBOOK_BALANCE = 0.1  # |дисбаланс| ≤ этого → 'баланс'; больше → перевес стороны
WALL_MULT = 5           # заявка — «стена», если её объём > среднего по стороне × этого

# ── СТРАТЕГИЯ №4 — ТРЕНД ПО НЕДЕЛЬНОМУ КАНАЛУ (14 сентября 2026) ─────────────
# Правила и механика — в trend.py. Вход: часовая свеча закрылась за максимумом или
# минимумом предыдущих 168 часов; стоп 3 ATR(24) от входа; выход — закрытие за
# встречным каналом 42 часа; цели нет.
#
# ОТКУДА ПАРАМЕТРЫ. Квант-разбор 14.09.2026 в лаборатории: 5 классов идей по 34–38
# вариантов на 11 инструментах, подбор на ранней части истории, экзамен на поздней,
# издержки BingX (комиссия, спред, проскальзывание, фактический фандинг). Эта
# стратегия — единственная, прошедшая объявленный заранее отбор И экзамен:
#   ETH  разработка 05.2024–12.2025: 77 сделок, +43R; экзамен 01–09.2026: 31 сделка, +23R;
#   прочность: канал 120…216 ч и стоп 2…4 ATR — плюс в каждом году; издержки ×3 — +56R.
#   SOL  отдельным отбором: разработка +27R, экзамен +17R.
#
# ВКЛЮЧЕНА НА ВСЕ 21 ИНСТРУМЕНТ — РЕШЕНИЕМ ВЛАДЕЛЬЦА, И ЦЕНА ЭТОГО НАЗВАНА. На истории
# (параметры 168/42/3 ATR, итог после издержек) стратегия В МИНУСЕ на всех пяти валютных
# парах и на нефти: у валюты сделка стоит 0.3–0.5R одной комиссии. Каждый сигнал
# показывает строку ниже, чтобы человек видел это до входа, а не после.
# ── ТРЕНД ВЫКЛЮЧЕН — 17 сентября 2026, решение владельца ─────────────────────
# Бот больше НЕ ОТКРЫВАЕТ новых позиций по этой стратегии. Причина словами не
# названа; в том же разговоре разбирался её профиль исходов (замер 16–17.09.2026,
# боевые 168/42/3 ATR, 13 монет + GOLD, 833 дня, 1612 сделок):
#   винрейт 30%, медиана сделки −1R, стопом закрывается 52% сделок;
#   до 1:1 доходит 19.5% сделок, до 1:2 — 11.9%, до 1:3 — 7.2%, до 1:4 и дальше — 5.1%;
#   эти 5.1% дают 59% всей прибыли, а десять лучших сделок из 1612 — 85% итога:
#   без них средняя сделка падает +0.254R -> +0.039R, то есть в ноль;
#   просадка модели −149R, худшая серия убыточных подряд — 56 сделок.
#
# ЧТО ИМЕННО ВЫКЛЮЧЕНО: только ВХОДЫ (trend.step с allow_entries=False). Задача
# monitor_trend в планировщике ОСТАЁТСЯ, и на это две причины, обе проверяемые:
#   1) уже открытые позиции модели надо довести до стопа или выхода по каналу —
#      иначе /trend навсегда показывал бы «открыта», а подписчик не узнал бы об
#      исходе (тот же довод, по которому 15.09 оставили работать track_signals);
#   2) задача двигает trend_state.last_bar вперёд. Сними её на неделю — и при
#      включении step пошёл бы по всем пропущенным свечам окна TREND_H1_LIMIT и
#      объявил бы вход по пробою, случившемуся дни назад, по цене, которой на
#      рынке уже нет.
# Правила, параметры и все тесты стратегии НЕ ТРОНУТЫ: True возвращает её в бой.
TREND_SIGNALS = False

TREND_CHANNEL_BARS = 168   # канал входа, часов (неделя)
TREND_EXIT_BARS = 42       # встречный канал выхода, часов
TREND_STOP_ATR = 3.0       # стоп в ATR от цены входа
TREND_ATR_BARS = 24        # ATR — среднее истинного диапазона за столько часов
# Сколько часовых свечей тянуть: 168 на канал + 24 на ATR + запас, чтобы простой бота
# до ~8 суток догонялся свеча за свечой, а не терялся.
TREND_H1_LIMIT = 400

# Замер стратегии по инструменту за всю доступную историю, после издержек BingX.
# Крипта — BingX 05.2024–09.2026 (кроме ETH/SOL/BTC — до 08.2026, фандинг с запасом);
# валюта, золото, нефть — Yahoo до запуска контракта + BingX после.
TREND_MEASURED = {
    "ETH": "+66R за 28 мес., прошла отбор и экзамен",
    "SOL": "+35R за 28 мес., прошла отбор и экзамен",
    "BTC": "+5R за 28 мес. — около нуля",
    "XRP": "+68R, почти всё — ралли конца 2024",
    "XLM": "+152R, почти всё — ралли конца 2024",
    "SUI": "+38R", "UNI": "+17R", "DOGE": "+15R", "ADA": "+12R", "LINK": "+9R",
    "AVAX": "−11R — в минусе", "AAVE": "−15R — в минусе", "LTC": "−56R — в минусе",
    "TON": "не мерилась — у контракта мало истории",
    "GOLD": "+2R — около нуля",
    "BRENT": "−10R — в минусе",
    "EURUSD": "−89R — в минусе, комиссия съедает",
    "GBPUSD": "−72R — в минусе, комиссия съедает",
    "AUDUSD": "−66R — в минусе, комиссия съедает",
    "USDCAD": "−50R — в минусе, комиссия съедает",
    "USDJPY": "−40R — в минусе, комиссия съедает",
}

# ── Планировщик (минуты) ────────────────────────────────────────────────────
ANALYZE_EVERY_MIN = 60  # обновление контекста/уровней
MONITOR_EVERY_MIN = 5   # проверка паттернов
ALERT_EVERY_MIN = 5     # проверка алертов «касание уровня»

# ── Строгость отбора: два ATR-фильтра, настраиваются через /settings ─────────
#
# Оба отвечают на один вопрос — «не входим ли мы вдогонку за ушедшим движением».
# Пружина по VSA торгуется ОТ уровня: сняли ликвидность фитилём и вернулись. Вход в
# конце размашистой свечи, далеко от уровня, — это уже погоня, и она в замерах теряет.
#
# 0 = фильтр выключен. По умолчанию оба выключены — это ровно то, что крутится в бою
# сейчас, и точка отсчёта всех замеров.
#
# MAX_ENTRY_DIST_ATR — «вход у уровня»: закрытие свечи пробоя не дальше N×ATR от
#   пробитого уровня. Зависит от КОНКРЕТНОГО уровня, поэтому проверяется внутри цикла
#   по уровням: один и тот же бар бывает «у уровня» для ближнего и «вдогонку» для
#   дальнего.
# MAX_RISK_ATR — «не входить вдогонку»: риск сделки (закрытие → экстремум свечи +
#   запас стопа) не больше N×ATR. Меряет РАЗМАХ свечи и от уровня не зависит вовсе,
#   поэтому считается один раз до перебора уровней.
#
# ЗАМЕР 19–20 августа 2026 (14 инструментов BingX, 833 дня часовых свечей, боевая
# конфигурация как точка отсчёта). «Свежая половина» — вторая половина истории,
# которую отбор порогов не видел; это самая честная оценка «чего ждать дальше».
# Все строки посчитаны в ОДНОЙ конвенции издержек — иначе колонки несравнимы.
#
#   настройка                  сигналов/мес   брутто R/сделку   нетто на свежей половине
#   оба выключены                    88            −0.044               −198.6 R
#   вход ≤ 0.05 ATR                  17            +0.121                +23.7 R
#   вход ≤ 0.075 ATR                 25            +0.139                 +9.2 R
#   вход ≤ 0.1 ATR                   31            +0.094                 −4.7 R
#   вход ≤ 0.15 ATR                  41            +0.055                −32.3 R
#   риск ≤ 0.5 ATR                   28            +0.084                +11.5 R
#   риск ≤ 0.75 ATR                  53            +0.051                −60.8 R
#   риск ≤ 1.0 ATR                   69            +0.006               −128.2 R
#   вход 0.05 + риск 0.5              9            +0.145                +28.1 R
#   вход 0.075 + риск 0.5            13            +0.164                +21.5 R
#   вход 0.1 + риск 0.5              16            +0.161                +32.7 R
#   вход 0.075 + риск 0.75           19            +0.179                +24.8 R
#
# Читается так: чем строже порог, тем лучше средняя сделка и во столько же раз меньше
# сигналов. Зависимость МОНОТОННАЯ в обеих колонках — это «доза — эффект», а не
# удачное ведро.
#
# СОЧЕТАНИЯ ИЗМЕРЕНЫ 20 августа (перебор `--sweep both` в лаборатории) и оказались
# ЛУЧШЕ каждого фильтра поодиночке — и по средней сделке, и на свежей половине. Это
# опровергло ожидание «дистанция арифметически меньше риска, значит вместе будет как
# только дистанция»: фильтры не вложены друг в друга. Порознь они оставляют 683 и 765
# сделок, вместе — 366; риск ≤ 0.5 срезает половину того, что пропускает вход ≤ 0.075.
# Прогон воспроизвёл обе одиночные строки до третьего знака — значит колонки сравнимы.
#
# И честная оговорка, без которой таблица врёт. ПРИБЫЛЬНЫМ движок это не делает.
# Плюс на свежей половине есть, но на калибровочной половине все эти пороги в минусе,
# и по всей истории целиком — тоже в минусе. Механизм известен: фильтры отбирают
# сделки у самого уровня, а у таких сделок УЗКИЙ стоп; издержки берутся в процентах
# от цены и делятся на риск, поэтому цена сделки в R растёт ровно тогда же, когда
# растёт качество отбора. Полная арифметика — в CLAUDE.md, «Замер 19 августа 2026».
_DEFAULTS = {
    "MAX_ENTRY_DIST_ATR": 0.0,  # вход у уровня: 0 = выключен
    "MAX_RISK_ATR": 0.0,        # не входить вдогонку: 0 = выключен
}
_SETTINGS_PATH = Path(__file__).parent / "settings.json"


def _load() -> dict:
    """Общие пороги: дефолты + то, что админ сохранил через /settings.

    Чужие ключи из файла ОТБРАСЫВАЕМ. В боевом settings.json лежат пороги, убранные
    из меню (VOL_MULT, MIN_RR и прочие) — без фильтра они вернулись бы в _runtime и
    всплыли в all_settings(), хотя код их оттуда уже не читает.
    """
    if _SETTINGS_PATH.exists():
        try:
            saved = json.loads(_SETTINGS_PATH.read_text(encoding="utf-8"))
            return {**_DEFAULTS, **{k: v for k, v in saved.items() if k in _DEFAULTS}}
        except Exception:
            return dict(_DEFAULTS)
    return dict(_DEFAULTS)


_runtime = _load()


def get(key: str):
    """Текущее значение настраиваемого порога (с учётом правок через /settings)."""
    return _runtime.get(key, _DEFAULTS.get(key))


def set_value(key: str, value) -> None:
    """Изменить порог и сохранить в settings.json. Используется командой /settings."""
    if key not in _DEFAULTS:
        raise KeyError(key)
    _runtime[key] = value
    _SETTINGS_PATH.write_text(
        json.dumps(_runtime, ensure_ascii=False, indent=2), encoding="utf-8"
    )


def all_settings() -> dict:
    """Все текущие настраиваемые пороги (для отображения в /settings)."""
    return dict(_runtime)


# Ключи, которые подписчик может переопределить персонально (через /settings).
# Это пороги, влияющие на отбор его сигналов; остальные константы — только в коде.
TUNABLE = tuple(_DEFAULTS)


def effective(overrides: dict | None = None) -> dict:
    """Действующие пороги с учётом персональных переопределений пользователя.

    База — общие значения (settings.json / дефолты, задаёт админ). Поверх неё
    кладём личные `overrides` пользователя (из БД). Ключи вне TUNABLE игнорируем.
    Если overrides пуст — вернётся ровно общий набор (как у всех «по умолчанию»).
    """
    base = {k: get(k) for k in _DEFAULTS}
    if overrides:
        base.update({k: v for k, v in overrides.items() if k in _DEFAULTS})
    return base
