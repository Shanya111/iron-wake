# iron-wake

Telegram-бот — торговый ассистент по 21 бессрочному фьючерсу BingX (14 крипто-пар, золото,
нефть Brent, 5 валютных пар). Шлёт сигналы по стратегиям, объясняет расклад (`/analyze`),
ставит алерты на уровни, ведёт журнал сделок. Доступ закрытый — по одобрению админа.

## ⚠️ Правило работы со стратегиями
**Перед изменением стратегии открой её файл в `памятки/стратегии/`. Если в просьбе не ясно,
о какой стратегии речь — переспроси, не смешивай.**
Что работает сейчас — только в `памятки/стратегии/`. Всё про «откачено», «выключено» и
замеры лежит в `памятки/журнал-экспериментов.md`: это история, а не правила.

## Действующие стратегии (на 26.09.2026)
- 🔴 **Ложный пробой**, правила 23 июня (`spring_june.py`): прокол уровня и возврат на объёме,
  стоп «фитиль + 1 ATR» → [ложный-пробой.md](памятки/стратегии/ложный-пробой.md)
- 💧 **ICT на часовиках** (`ict.py`): свип пула, импульс с разрывом, слом структуры, вход по
  рынку → [ict.md](памятки/стратегии/ict.md)
- 📈 **Пробой сильного уровня** (`breakout.py`): идёт за пробоем; шлёт по крипте, золоту и
  нефти, по валюте молчит → [пробой-уровня.md](памятки/стратегии/пробой-уровня.md)
- Общее для всех (уровни, направление, ATR) → [общая-основа.md](памятки/стратегии/общая-основа.md)

Ни одна стратегия не доказала прибыльности на истории. Цена каждой записана в её файле.

**Не в бою, но в коде** (включаются одной константой): сентябрьский движок ложного пробоя
(`pattern_detector.py`), тренд №4 (`trend.py`), фильтры строгости, скользящий час →
[спящий-код.md](памятки/спящий-код.md)

## Стек
Python + aiogram 3, SQLite (`database.py`), APScheduler (один планировщик в `scheduler.py`),
CCXT + pandas (данные только с BingX-фьючерсов), OpenRouter (чат, разбор, NL-роутер команд).

## Памятки
| о чём | файл |
|---|---|
| стек, структура файлов, принципы, автор | [устройство.md](памятки/устройство.md) |
| 21 инструмент, BingX, тикеры, форекс-расписание | [инструменты-и-данные.md](памятки/инструменты-и-данные.md) |
| фоновые задачи, ведение сигнала до исхода | [планировщик.md](памятки/планировщик.md) |
| команды, `/analyze`, `/subscribe`, алерты, журнал сделок, доступ, рассылка | [команды.md](памятки/команды.md) |
| таблицы `bot.db` | [база-данных.md](памятки/база-данных.md) |
| ИИ: чат, NL-роутер, комментарий к сигналу | [llm.md](памятки/llm.md) |
| оплата `/pay` | [оплата.md](памятки/оплата.md) |
| тесты и лаборатория замера `../iron-wake-lab` | [тесты-и-лаборатория.md](памятки/тесты-и-лаборатория.md) |
| история всех экспериментов по датам | [журнал-экспериментов.md](памятки/журнал-экспериментов.md) |
| деплой (сам запускается по пушу в `main`) | [DEPLOY.md](DEPLOY.md) |
| схема функций бота | [схема.md](схема.md) |

## Принципы
- Простой и читаемый код с комментариями на русском.
- Изменил правило стратегии → обнови её файл в `памятки/стратегии/`, а старую версию
  и замер запиши датой в журнал.
- Тесты: `python tests/test_engine.py` (в Windows с `PYTHONIOENCODING=utf-8`).
